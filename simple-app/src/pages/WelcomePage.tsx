import React, { ReactElement } from 'react'

interface Props {
    
}

export default function WelcomePage({}: Props): ReactElement {
    return (
        <div>
            Welcome:
        </div>
    )
}
